public/
resources/
.hugo_build.lock
.DS_Store
node_modules/
.netlify/
*.log

# Private / raw source materials — internal only
/*.pdf

# Email attachment junk
ATT*.html
# Raw flyer source (kept locally, not tracked)
/Flyer_NECB*.png
/Flyer_NECB*.pdf
/Flyer_NECB*.ai
/iscb-necb-banner.png

# Rendered review artifacts (regenerable via scripts/render_pdf.py)
/docs/review/build/

# Submitted-abstract data — never commit
/NECB-2026-Submission_*.csv
/NECB2026_submissions*.zip
/NECB2026_submissions/
# Any exported / derived review CSVs, PDFs, or PPTs at the repo root
/submissions_*.csv
/assignments*.csv
/scores*.csv
/ranked*.csv
/reviewer_urls*.csv
/*.pptx
# Extracted PDF folders, in case someone lands them outside /docs/review/build/
/pdfs/
